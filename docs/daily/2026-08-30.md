# Daily Project Log — 2026-08-30

**Project:** Friday
**Agent/Owner:** ChatGPT / Owner
**Branch:** `master` (recorded from clean worktree tracking `origin/master`)
**HEAD before this log update:** `cadec57`
**Working tree before this log update:** clean

## Done Today
- Verified live LG TV Wake-on-LAN from Friday; owner confirmed the TV physically powered on.
- Inspected current Friday LG/webOS capabilities from the real code/runtime without changing additional TV state.
- Confirmed existing Friday TV tools cover power, volume, app launch, YouTube search/playback, and allowlisted remote-button control.
- Confirmed the underlying `pywebostv` path can also read current app, volume/mute, audio output, and system/software information, but Friday does not yet expose a dedicated read-only `tv_status` tool.
- Performed read-only LAN discovery and found the current webOS endpoint; MAC comparison matched the known LG TV.
- Confirmed Friday runtime is still configured to an older TV endpoint, reproducing the known DHCP/IP-drift problem.

## Current State
- `master` remains the stable baseline; Phase 0–10 are not merged into `master`.
- PR #2 Camera Discovery is the independent ready PR.
- PR #3–#13 are stacked Draft PRs with live/evidence gates still authoritative.
- Historical PR #1 is closed and was not merged.
- TV Wake-on-LAN works, while direct webOS read/control remains blocked by stale endpoint configuration and pending Phase 0 key rotation/re-pair.

## Decisions Made
- Keep Home Assistant as the canonical smart-home control plane; direct LG webOS remains compatibility/exploration until HA parity is proven.
- Do not depend on a fixed TV IP for the long-term control path.
- Add a read-only `tv_status` capability after Phase 0 is closed, using authoritative live state where available.

## Open
- Phase 0 LG client-key rotation/re-pair and final TV/API live regression.
- Add read-only TV status coverage after the Phase 0 live gate.
- Replace fixed endpoint dependence with Home Assistant or deterministic endpoint resolution.
- Continue closing the live/evidence gates for the existing stacked PRs in dependency order.

## Blocked
- Phase 0 final TV gate is blocked until the historical LG webOS paired key is rotated/re-paired and the replacement key is stored only in machine-local configuration.
- Real Home Assistant entity/write/scene validation remains blocked until a real HA instance is onboarded.
- Phase 7 IR/legacy-AC live gate remains blocked on RM4 Mini or other supported IR hardware plus a real AC pilot.

## Next
1. When the owner is physically available at the TV, rotate/re-pair the LG webOS client key.
2. Verify read-only current app, volume/mute, audio output, and system info with the replacement key.
3. Run the Phase 0 TV/API live regression and close Gate 0 only if all required evidence passes.
4. Then continue Phase 1/2 runtime evidence and Phase 4/5 Home Assistant onboarding in dependency order.

## Do Not Repeat
- Do not reopen historical PR #1; the reconciled stacked PR chain is the current source of truth.
- Do not re-investigate whether the TV IP can drift; live evidence already reproduced the DHCP/IP-drift condition.
- Do not put LG client keys, Home Assistant tokens, raw IR codes, or other secrets in source or daily logs.

## Evidence
- Baseline before daily-log update: `origin/master @ cadec57`.
- PRs: #2 open ready; #3–#13 open Draft; #1 closed unmerged.
- Live TV: Wake-on-LAN command sent by Friday; owner confirmed physical power-on.
- Read-only LAN evidence: current webOS endpoint discovered and MAC matched the known LG TV; exact network identifiers intentionally omitted from this log.
- Relevant docs: `docs/FRIDAY_BRANCH_PR_READINESS_2026-08-28.md`, `docs/FRIDAY_DEVELOPMENT_PLAN_2026-08-26.md`, `handoff/2026-08-26-z-friday-current-state-docs-refresh.md`.
- Deploy/runtime: no deployment; no Phase 0–10 merge performed in this daily-log update.
